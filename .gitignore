# Node
node_modules/

# Wasmer Shipit local build output
.shipit/

# Capacitor web build output (regenerated from repo-root static files via `npm run copy:web`)
www/

# Generated native copies of the web assets / plugin config (regenerated by `cap sync`)
android/app/src/main/assets/public/
android/app/src/main/assets/capacitor.config.json
android/app/src/main/assets/capacitor.plugins.json

# Android local + build artifacts
android/local.properties
android/.gradle/
android/build/
android/app/build/
android/capacitor-cordova-android-plugins/

# Build outputs / signing (never commit signing keys or passwords)
*.apk
*.aab
*.keystore
*.jks
android/keystore.properties
