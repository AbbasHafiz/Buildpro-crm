# Generated by Anybuild v0.26.5 — https://anybuild.run
# Wasmer GitHub deploy uses Project preset "node-static".
# Serves the prebuilt PWA from public/ (Capacitor package.json is ignored for runtime).

load("//anybuild/tools:node_static.bzl", "nodestatic_build", "nodestatic_config")
load("//anybuild/tools:staticfile.bzl", "staticfile_serve")

config = nodestatic_config(
    schema = 1,
    sws_version = "2.38.0",
    static_dir = "public",
    node_package_manager = "npm",
    node_server = "node",
    node_version = "24",
)

build = nodestatic_build(config)

staticfile_serve(config, build, name = "buildpro-crm")
